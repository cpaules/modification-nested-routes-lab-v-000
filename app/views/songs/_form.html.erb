<%= form_for @song  do |f| %>
  <%= artist_select(@song, @song.artist) %>
  <%= f.hidden_field :artist_id %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <br />

  <%= f.submit %>
<% end %>
